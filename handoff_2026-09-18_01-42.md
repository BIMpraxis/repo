# Traspaso — REPO (repo-oc) — 2026-09-18 01:42

## Objetivo de la próxima sesión
Retomar la publicación en npm cuando la cuenta se desbloquee, y/o continuar mejoras del CLI. La funcionalidad pedida ya está hecha, instalada y validada por el humano; lo único pendiente es la vía npm (en pausa ajena al proyecto).

## Estado del trabajo

### Completado — validado por el humano
- **Entrada «+ Nuevo repo…»** en la TUI: confirmada funcionando en su equipo.
- **Instalación desde Git**: confirmada funcionando con la línea del README (`npm install -g github:BIMpraxis/repo; Remove-Item "$env:APPDATA\npm\repo.ps1" -Force`), ejecutada por él mismo.

### Completado — pendiente de validación
- Nada funcional. La documentación (PRD, CONTEXT, RELEASE, READMEs) quedó actualizada y commiteada.

### En curso
- Nada a medias en el código.

### Bloqueado
- **Publicación en npm**, por dos motivos independientes:
  1. La cuenta npm está en **solo lectura 72 h** (lo dispara el uso de un código de recuperación de 2FA; protección preventiva de npm). Se levanta sola; sin trámites.
  2. No existe todavía el **publicador de confianza** en npm.

## Detalles importantes
- Paquete: `repo-oc` (sin scope), público, responsable `bimpraxis <juliopablo@bimpraxis.com>`. Última versión publicada: **0.1.1** (2026-09-17), a mano.
- `bin/repo.mjs` implementa la TUI; la opción nueva está al final de la lista y usa `promptText()` + `createRepoFlow()`.
- Comprobaciones locales: `npm run check` (`scripts/check.mjs`): sintaxis, `bin` existente, referencias `%~dp0` sin destino, guard sin TTY, contenido exacto del tarball (5 archivos) y versión ↔ CHANGELOG.
- `release.yml` está en **`workflow_dispatch`** (solo manual) para no generar ejecuciones en rojo mientras falta el publicador. Al reactivar npm hay que devolverle el disparo por `push` a `main`.
- Las herramientas de `semantic-release` **no** están en `devDependencies`: el workflow las instala con `npm install --no-save`. Motivo: que la instalación desde Git sea ligera (12 paquetes en vez de 446).
- Trampa resuelta: `setup-node` con `registry-url` provocaba `EINVALIDNPMTOKEN`. Sin esa línea, el error pasó a `404 OIDC token exchange error - package not found`, que es el esperado mientras no exista el publicador de confianza.
- Diagnóstico de 2FA hecho con PowerShell: la cuenta de Windows es **local** (`BIMPRAXIS2\USER`, `PrincipalSource: Local`) y el sistema es **Windows 10**. La experiencia nativa de passkeys es de Windows 11; el correo de Windows no influye. El challenge con «Windows Hello» en Chrome se cuelga en «Estamos comprobando que eres tú».
- El bloqueo de 72 h **solo** lo disparan el cambio de email y el uso de un código de recuperación: reintentar el challenge no lo reinicia y **no** hay que gastar más códigos.
- El tag `v0.1.1` en `39e0b94` marca la versión publicada (su `bin/repo.mjs` y `package.json` son idénticos a los del tarball de npm).

## Decisiones y respuestas del humano

### Decisiones tomadas
- Quería la opción «Nuevo repo» en la TUI, al final de la lista, creando la carpeta y lanzando OpenCode (volcada al PRD/README).
- Release con **semantic-release en `main`** y CHANGELOG automático (volcada a `docs/RELEASE.md`).
- Commitear `opencode.json` y `.ignore` junto al resto (hecho).
- 2FA de npm: **Plan A** (un código del móvil y nunca más) — resultó inviable por lo descrito arriba (no se pudo completar).
- **Rechaza** abrir tickets a soporte y comprar una llave USB. Exige otra vía (volcada a `docs/RELEASE.md` como alternativas sin probar).
- **Exige permiso antes de instalar nada en su equipo** (me lo salté una vez y hubo que desinstalar; reforzado en el comportamiento del agente).
- Distribución sin npm: instalación desde Git (volcada al PRD, READMEs y RELEASE).
- Autorizó explícitamente actualizar el **README** (excepción a la norma de que es responsabilidad suya).
- Los traspasos (`handoff_*.md`) se **commitean** en el repositorio: no se ignoran ni se borran solos, y no hay que volver a preguntarlo (2026-09-18).
- Umbral del plugin zonalista subido a **1.000.000** tokens para esta sesión.

### Respuestas a preguntas del agente
- «¿Cómo autentica el release?» → le preocupa el ritual de 2FA; quiere lo más robusto con menos pasos y que el CI publique sin él.
- «¿Publish directo o con aprobación?» → **directo desde el tag**.
- «¿CHANGELOG a mano o generado?» → **generado**.
- «¿Commitear la config de graft?» → **sí**.
- «¿Cambiamos 2FA a security key?» → eligió Plan A; se abandonó tras el bloqueo.

### Instrucciones y preferencias
- **Leer y obedecer los AGENTS.md en cada prompt** y confirmarlo expresamente.
- **Brevedad**: respuestas cortas; se queja de mensajes largos que no lee.
- **No quedarse vigilando procesos** con esperas: prohibido dejar comandos «en espera» largos. Consultar el estado de forma puntual, nunca en bucle.
- **Guiado de interfaces**: nunca dar un paso sin verificar la documentación de esa pantalla concreta; un paso por mensaje; texto literal del control y su ubicación; si lo que ve no coincide, parar. Está escrito en el PRD como informe de negligencia.
- No commitear ni empujar sin petición expresa.
- Cuando pide algo, quiere acción: no marearle con alternativas ni preguntas de más.

### Preguntas sin responder
- Si npm permite tener TOTP (Microsoft Authenticator) junto a una security key, o hay que sustituir una por otra.
- Si el challenge de 2FA funciona en Edge en su equipo.

## Decisiones abiertas
- Cómo desbloquear el 2FA de npm sin tickets ni llave USB (probablemente probar el challenge en Edge; o el gestor de passkeys de Chrome).
- Si se reactiva el disparo por push de `release.yml` y se publica la 0.2.0.

## Próximos pasos
1. Cuando pase el bloqueo (~72 h desde la noche del 2026-09-17): probar el challenge de 2FA en **Edge** y, si entra, crear el publicador de confianza (`https://www.npmjs.com/package/repo-oc/access` → Trusted Publisher → GitHub Actions: `BIMpraxis` / `repo` / `release.yml` / entorno vacío / marcar `npm publish`).
2. Devolver el disparo por `push` a `main` en `release.yml` y lanzar el release para publicar la **0.2.0** con provenance.
3. Confirmar que el 0.2.0 publicado es el que se espera y actualizar el equipo si procede.

## Archivos relevantes
- `bin/repo.mjs` — TUI y lógica de «+ Nuevo repo…».
- `scripts/check.mjs` — comprobaciones previas.
- `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.releaserc.json`.
- `PRD.md`, `CONTEXT.md`, `CHANGELOG.md`, `docs/RELEASE.md`.
- `README.md`, `README.en.md`.

## Comportamiento del agente
- Leer y obedecer los AGENTS.md (global y de proyecto) en cada prompt, confirmándolo.
- Pedir permiso antes de instalar o cambiar cualquier cosa en su equipo.
- No dejar comandos en espera ni bucles de sondeo.
- Un paso por mensaje en tareas de interfaz, con texto literal verificado.
- Ser breve.

## Skills sugeridas
- `cierra-ciclo` — para volver a cerrar el ciclo cuando se publique la 0.2.0.
- `traspaso` — al retomar esta sesión.

## Referencias externas
- `PRD.md` (incluye el informe de negligencia y el contrato de distribución).
- `docs/RELEASE.md` (runbook, estado de npm y trampas verificadas).
- Commits: `73eb38c` (CI inicial), `6fe49d3` (registry-url y permisos), `f5d4ac9` (distribución por Git y release manual), `398ae8f` (feature).
- Tag: `v0.1.1`.
- npm: `https://www.npmjs.com/package/repo-oc`.

## Notas
- Sin secretos: no se ha leído ni escrito ningún token; el de `%USERPROFILE%\.npmrc` está caducado (E401) y no se usa.
- No hay procesos lanzados por el agente que sigan vivos.
